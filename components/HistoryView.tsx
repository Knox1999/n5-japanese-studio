'use client';
import { BarChart3, Trophy } from 'lucide-react';
import type { MockAttempt } from '@/lib/types';
export default function HistoryView({history}:{history:MockAttempt[]}){return <div className="space-y-5"><section className="study-header tone-history"><div><div className="section-kicker">Performance History</div><h1>Your saved mock attempts</h1><p className="font-bn">এই browser-এ save থাকা real attempt-গুলোই দেখানো হচ্ছে।</p></div><BarChart3 className="header-big-icon"/></section>{!history.length?<div className="empty-state"><Trophy/><b>No mock history yet</b><p>Complete a mock test to start building your performance trend.</p></div>:<div className="grid gap-3">{history.map((x,i)=><article className="history-card" key={`${x.date}-${i}`}><div className="history-score">{x.score}%</div><div><b>{x.label||(x.scope==='n5-full'?'JLPT N5 Full Simulation':`Lesson ${String(x.lesson).padStart(2,'0')}`)}</b><p>{x.correct}/{x.total} correct · {new Date(x.date).toLocaleString()}</p></div><div className="history-bar"><i style={{width:`${x.score}%`}}/></div></article>)}</div>}</div>}
