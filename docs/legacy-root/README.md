# Legacy root artifacts

These files belonged to the old single-file/GitHub Pages build and are archived only for history. The production site is generated from the Next.js `app/` source and `public/` assets.
