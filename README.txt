N5 Japanese Studio — V59 Full Redesign

1. Copy styles/v59-full-redesign.scss to your repo's styles/ folder.
2. Replace app/layout.tsx with the included app/layout.tsx.
3. Run:
   npm run lint:types
   npm run build

The redesign is intentionally a final override layer after v58-design-system.scss,
so existing app logic and data behavior remain unchanged.
